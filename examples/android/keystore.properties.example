# Copy to keystore.properties (gitignored) or set RELEASE_* in .env.
# Generate once: bash scripts/generate-release-keystore.sh
storeFile=openshouter-release.keystore
storePassword=
keyAlias=openshouter
keyPassword=
